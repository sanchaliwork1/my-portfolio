import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import { BackgroundTransitions } from "@/components/background-transitions"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Sanchali Sarkar | AI & ML Engineer",
  description:
    "Portfolio of Sanchali Sarkar - CSE Student specializing in Artificial Intelligence and Machine Learning",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`font-sans antialiased`}>
        <BackgroundTransitions />
        {children}
        <Analytics />
      </body>
    </html>
  )
}
